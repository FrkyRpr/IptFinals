create table info (

	 id INTEGER  primary key AUTOINCREMENT,
	 username varchar(500),
	 address varchar(500),
	 email varchar(500),
	 phone int not null,
	 age int not null,
	 gender varchar(500),
	 lang varchar(500),
	 msg varchar(500)
);
